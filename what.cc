#include <iostream>
#include <vector>
int main(void){
    // using std::string,std::cout;
    // string name = "Hello";
    // cout << name;
 std::vector<int> arr;
 for (int i = 0; i < 10; i++)
 {
    int x ;
    std::cin >> x;
    arr.push_back(x);
 }
 arr.size();
 
}