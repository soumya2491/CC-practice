#include <iostream>
#include <cmath>
int main(){
    int a = 60 ;
    int b = 20;
// //    std::max(a,b);
//    std::cout<<std::pow(2,3);
//   std::cout << std::abs(-3);
for (int i = 0; i < 5; i++)
{
    // std::max(arr[i], arr[1]);
}

// std::max();
}